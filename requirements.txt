# Fonte canônica de dependências: pyproject.toml
# Este arquivo é gerado a partir dele e mantido apenas por compatibilidade.
#   pip install -e .          (runtime)
#   pip install -e ".[dev]"   (runtime + testes/lint)
-e .
